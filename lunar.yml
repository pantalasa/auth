# Lunar component self-declaration: criticality tier + where monitoring lives.
# criticality drives ticket enforcement (production = required/blocks merge).
criticality: production
datadog:
  service: auth
  dashboard_id: nir-grm-5ge
